# Final Project Submission

The entirety of the source code can be found online at `https://github.com/SEGBK/term-project`.

In this submission, you will find:

The `lib` directory contains the source code for the library.
 - This is an exact clone of the `lib` branch of our repo.
 - Only the final jar file is provided in this. It is compiled on JDK7.
 - No *.class files are included.

The `app` directory contains the source code for the app.
 - This is an exact clone of the `master` branch of our repo.

The `Final Project.pdf` file contains the report and in it you will find explainations
of all the code and related texts.
